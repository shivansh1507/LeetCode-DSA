import java.util.Scanner;
import java.util.*;

public class functions {
    public static void main(String args[]) {
        Scanner sc = new Scanner(System.in);
        String str1 = sc.nextLine();
        // String str = sc.nextLine();
        // System.out.print(str1.equalsIgnoreCase(str));
        // System.out.print(str1.contains(str));
        // System.out.print(str1.indexOf(str));
        // System.out.print(str1.substring(0, 6));
        // System.out.print(str1.substring(0, 1).toUpperCase() + str1.substring(1));
    }
}
